<p style="color: green"><%= notice %></p>

<h1><%= t(".items") %></h1>

<div id="items">
  <ul>
    <% @items.each do |item| %>
    <li><%=link_to item.name, item %></li>
    <% end %>
  </ul>
</div>

<%= link_to t(".new_item"), new_item_path %>
